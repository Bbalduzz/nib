# Wheredoiwatchit

A macOS menu bar application built with [nib](https://github.com/Bbalduzz/nib).

## Development

Run the app in development mode:

```bash
python src/main.py
```

## Building

Build a standalone `.app` bundle:

```bash
nib build
```

The app will be created in `dist/Settings Demo.app`.
